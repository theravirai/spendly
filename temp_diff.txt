diff --git a/.agents/agents/spendly-test-runner/SKILL.md b/.agents/agents/spendly-test-runner/SKILL.md
index 7c4d8c1..2dc4cf4 100644
--- a/.agents/agents/spendly-test-runner/SKILL.md
+++ b/.agents/agents/spendly-test-runner/SKILL.md
@@ -133,14 +133,14 @@ Always check test output for signals of these common Spendly mistakes:
 - `return "error string"` in routes → must use `abort()`
 - App running on port 5000 → must be 5001
 - Any JS framework imports → only vanilla JS allowed
-- `database/db.py` helpers assumed to exist before they are implemented → check step status in CLAUDE.md
+- `database/db.py` helpers assumed to exist before they are implemented → check step status in AGENTS.md.md
 
 ---
 
 ## Escalation Policy
 
 - If tests cannot run due to import errors or missing dependencies, diagnose and report — do NOT attempt to install new packages
-- If a test file exercises a stub route that is not yet implemented per CLAUDE.md, flag this clearly: "This test targets a stub route — implementation must precede testing"
+- If a test file exercises a stub route that is not yet implemented per AGENTS.md.md, flag this clearly: "This test targets a stub route — implementation must precede testing"
 - If results are ambiguous, re-run with `pytest -s` for full output before concluding
 
 ---
\ No newline at end of file
diff --git a/.agents/skills/code-review-feature/SKILL.md b/.agents/skills/code-review-feature/SKILL.md
index 8a72379..f88ef35 100644
--- a/.agents/skills/code-review-feature/SKILL.md
+++ b/.agents/skills/code-review-feature/SKILL.md
@@ -33,7 +33,7 @@ context:
 
 **spendly-security-reviewer** receives:
 - The combined diff from the pre-flight check
-- Spec file for context: `.claude/specs/$ARGUMENTS.md`
+- Spec file for context: `.agents/specs/$ARGUMENTS.md`
 - Source files to reference: `app.py` and 
   `database/` directory
 - Instruction: Review only the changed code for 
@@ -42,7 +42,7 @@ context:
 
 **spendly-quality-reviewer** receives:
 - The combined diff from the pre-flight check
-- Spec file for context: `.claude/specs/$ARGUMENTS.md`
+- Spec file for context: `.agents/specs/$ARGUMENTS.md`
 - Source files to reference: `app.py`, `database/` 
   directory, and `templates/` directory
 - Instruction: Review only the changed code for quality, 
@@ -103,7 +103,7 @@ approves.
   both must run in parallel
 - Do NOT skip the pre-flight diff check
 - Do NOT proceed if the spec file at 
-  `.claude/specs/$ARGUMENTS.md` does not exist — 
+  `.agents/specs/$ARGUMENTS.md` does not exist — 
   report it and stop
 - If either subagent fails or returns no output, 
   report it and do not present a partial review 
diff --git a/.agents/skills/test-feature/SKILL.md b/.agents/skills/test-feature/SKILL.md
index f004544..8e6c868 100644
--- a/.agents/skills/test-feature/SKILL.md
+++ b/.agents/skills/test-feature/SKILL.md
@@ -11,9 +11,9 @@ If no argument is provided, stop immediately and say:
 "Please provide a spec name. Usage: /test-feature 
 <spec-name> e.g. /test-feature 05-backend-connection"
 
-If `.claude/specs/$ARGUMENTS.md` does not exist, stop 
+If `.agents/specs/$ARGUMENTS.md` does not exist, stop 
 immediately and say:
-"Spec file not found at .claude/specs/$ARGUMENTS.md. 
+"Spec file not found at .agents/specs/$ARGUMENTS.md. 
 Please check the spec name and try again."
 
 ---
@@ -24,7 +24,7 @@ Invoke the **spendly-test-writer** subagent with the
 following context:
 
 - Spec file to base tests on: 
-  `.claude/specs/$ARGUMENTS.md`
+  `.agents/specs/$ARGUMENTS.md`
 - Source files to read for structure:
   - `app.py`
   - `database/` directory
@@ -51,7 +51,7 @@ context:
 - Test file to execute:
   `tests/test_$ARGUMENTS.py`
 - Spec file for context:
-  `.claude/specs/$ARGUMENTS.md`
+  `.agents/specs/$ARGUMENTS.md`
 - Source files to analyze against when diagnosing 
   failures:
   - `app.py`
diff --git a/app.py b/app.py
index 8f178d6..966882f 100644
--- a/app.py
+++ b/app.py
@@ -1,4 +1,6 @@
 import sqlite3
+from datetime import date, timedelta
+import calendar
 
 from flask import Flask, flash, redirect, render_template, request, session, url_for
 from werkzeug.security import check_password_hash, generate_password_hash
@@ -107,21 +109,66 @@ def profile():
     if not user_id:
         return redirect(url_for("login"))
 
-    user_info = get_user_by_id(user_id)
-
-    summary_stats = get_summary_stats(user_id)
-
-    recent_expenses = get_recent_transactions(user_id, limit=10)
+    # Extract query parameters
+    preset = request.args.get("preset")
+    start_date_str = request.args.get("start_date", "").strip()
+    end_date_str = request.args.get("end_date", "").strip()
+
+    # Determine preset if not explicitly provided but dates are
+    if not preset:
+        if start_date_str or end_date_str:
+            preset = "custom"
+        else:
+            preset = "all"
+
+    today = date.today()
+    start_date = None
+    end_date = None
+
+    if preset == "7d":
+        start_date = today - timedelta(days=6)
+        end_date = today
+    elif preset == "30d":
+        start_date = today - timedelta(days=29)
+        end_date = today
+    elif preset == "this-month":
+        start_date = today.replace(day=1)
+        _, last_day = calendar.monthrange(today.year, today.month)
+        end_date = today.replace(day=last_day)
+    elif preset == "custom":
+        if start_date_str:
+            try:
+                start_date = date.fromisoformat(start_date_str)
+            except ValueError:
+                start_date = None
+        if end_date_str:
+            try:
+                end_date = date.fromisoformat(end_date_str)
+            except ValueError:
+                end_date = None
+
+    # Convert date objects to ISO string representation for query execution
+    start_date_query = start_date.isoformat() if start_date else None
+    end_date_query = end_date.isoformat() if end_date else None
+
+    # If preset is custom and both dates are empty, treat as preset = all
+    if preset == "custom" and not start_date_query and not end_date_query:
+        preset = "all"
 
-    user_id = session.get("user_id")
-    category_breakdown = get_category_breakdown(user_id)
+    user_info = get_user_by_id(user_id)
+    summary_stats = get_summary_stats(user_id, start_date=start_date_query, end_date=end_date_query)
+    recent_expenses = get_recent_transactions(user_id, limit=10, start_date=start_date_query, end_date=end_date_query)
+    category_breakdown = get_category_breakdown(user_id, start_date=start_date_query, end_date=end_date_query)
 
     return render_template(
         "profile.html",
         user_info=user_info,
         summary_stats=summary_stats,
         recent_expenses=recent_expenses,
-        category_breakdown=category_breakdown
+        category_breakdown=category_breakdown,
+        preset=preset,
+        start_date=start_date_query,
+        end_date=end_date_query
     )
 
 
diff --git a/database/queries.py b/database/queries.py
index a1b79cc..acf894e 100644
--- a/database/queries.py
+++ b/database/queries.py
@@ -42,31 +42,45 @@ def get_user_by_id(user_id):
     finally:
         conn.close()
 
-def get_summary_stats(user_id):
+def get_summary_stats(user_id, start_date=None, end_date=None):
     """Returns a dict with total_spent, transaction_count, and top_category."""
     conn = get_db()
     try:
         # Total spent and transaction count
-        stats_row = conn.execute(
-            "SELECT SUM(amount) as total_spent, COUNT(*) as transaction_count FROM expenses WHERE user_id = ?",
-            (user_id,)
-        ).fetchone()
+        query = "SELECT SUM(amount) as total_spent, COUNT(*) as transaction_count FROM expenses WHERE user_id = ?"
+        params = [user_id]
+        if start_date:
+            query += " AND date >= ?"
+            params.append(start_date)
+        if end_date:
+            query += " AND date <= ?"
+            params.append(end_date)
+            
+        stats_row = conn.execute(query, params).fetchone()
         
         total_spent = stats_row["total_spent"] if stats_row["total_spent"] is not None else 0.0
         transaction_count = stats_row["transaction_count"] if stats_row["transaction_count"] is not None else 0
         
         # Top category by sum of amounts
-        top_cat_row = conn.execute(
-            """
+        top_cat_query = """
             SELECT category, SUM(amount) as total_amount
             FROM expenses
             WHERE user_id = ?
+        """
+        top_cat_params = [user_id]
+        if start_date:
+            top_cat_query += " AND date >= ?"
+            top_cat_params.append(start_date)
+        if end_date:
+            top_cat_query += " AND date <= ?"
+            top_cat_params.append(end_date)
+        top_cat_query += """
             GROUP BY category
             ORDER BY total_amount DESC, category ASC
             LIMIT 1
-            """,
-            (user_id,)
-        ).fetchone()
+        """
+        
+        top_cat_row = conn.execute(top_cat_query, top_cat_params).fetchone()
         
         top_category = top_cat_row["category"] if top_cat_row else "—"
         
@@ -78,20 +92,26 @@ def get_summary_stats(user_id):
     finally:
         conn.close()
 
-def get_recent_transactions(user_id, limit=10):
+def get_recent_transactions(user_id, limit=10, start_date=None, end_date=None):
     """Returns a list of dicts, each with date, description, category, amount."""
     conn = get_db()
     try:
-        rows = conn.execute(
-            """
+        query = """
             SELECT date, description, category, amount
             FROM expenses
             WHERE user_id = ?
-            ORDER BY date DESC, id DESC
-            LIMIT ?
-            """,
-            (user_id, limit)
-        ).fetchall()
+        """
+        params = [user_id]
+        if start_date:
+            query += " AND date >= ?"
+            params.append(start_date)
+        if end_date:
+            query += " AND date <= ?"
+            params.append(end_date)
+        query += " ORDER BY date DESC, id DESC LIMIT ?"
+        params.append(limit)
+        
+        rows = conn.execute(query, params).fetchall()
         
         return [
             {
@@ -105,15 +125,21 @@ def get_recent_transactions(user_id, limit=10):
     finally:
         conn.close()
 
-def get_category_breakdown(user_id):
+def get_category_breakdown(user_id, start_date=None, end_date=None):
     """Returns a list of dicts, each with category, name, amount, percentage, pct, and class."""
     conn = get_db()
     try:
         # Get total spend first
-        total_row = conn.execute(
-            "SELECT SUM(amount) as total_spent FROM expenses WHERE user_id = ?",
-            (user_id,)
-        ).fetchone()
+        query_total = "SELECT SUM(amount) as total_spent FROM expenses WHERE user_id = ?"
+        params_total = [user_id]
+        if start_date:
+            query_total += " AND date >= ?"
+            params_total.append(start_date)
+        if end_date:
+            query_total += " AND date <= ?"
+            params_total.append(end_date)
+            
+        total_row = conn.execute(query_total, params_total).fetchone()
         
         total_spent = total_row["total_spent"] if total_row["total_spent"] is not None else 0.0
         
@@ -121,16 +147,24 @@ def get_category_breakdown(user_id):
             return []
             
         # Get amounts grouped by category ordered by amount desc
-        rows = conn.execute(
-            """
+        query_breakdown = """
             SELECT category, SUM(amount) as total_amount
             FROM expenses
             WHERE user_id = ?
+        """
+        params_breakdown = [user_id]
+        if start_date:
+            query_breakdown += " AND date >= ?"
+            params_breakdown.append(start_date)
+        if end_date:
+            query_breakdown += " AND date <= ?"
+            params_breakdown.append(end_date)
+        query_breakdown += """
             GROUP BY category
             ORDER BY total_amount DESC
-            """,
-            (user_id,)
-        ).fetchall()
+        """
+        
+        rows = conn.execute(query_breakdown, params_breakdown).fetchall()
         
         breakdown = []
         for row in rows:
diff --git a/static/css/profile.css b/static/css/profile.css
index 263fae3..77d5718 100644
--- a/static/css/profile.css
+++ b/static/css/profile.css
@@ -369,6 +369,146 @@
     color: var(--ink-muted);
 }
 
+/* ------------------------------------------------------------------ */
+/* Filter Card & Controls                                             */
+/* ------------------------------------------------------------------ */
+
+.filter-card {
+    display: flex;
+    flex-direction: column;
+    gap: 1.25rem;
+    padding: 1.25rem 1.5rem;
+}
+
+.filter-presets {
+    display: flex;
+    gap: 0.5rem;
+    flex-wrap: wrap;
+    border-bottom: 1px solid var(--border-soft);
+    padding-bottom: 0.75rem;
+}
+
+.preset-btn {
+    font-size: 0.85rem;
+    font-weight: 500;
+    color: var(--ink-muted);
+    padding: 0.35rem 0.75rem;
+    border-radius: var(--radius-sm);
+    background: transparent;
+    border: 1px solid transparent;
+    transition: all 0.2s ease;
+}
+
+.preset-btn:hover {
+    color: var(--ink);
+    background: var(--border-soft);
+}
+
+.preset-btn.active {
+    color: var(--accent);
+    background: var(--accent-light);
+    font-weight: 600;
+}
+
+.filter-form {
+    display: flex;
+    flex-wrap: wrap;
+    align-items: flex-end;
+    gap: 1.25rem;
+}
+
+.filter-group {
+    display: flex;
+    flex-direction: column;
+    gap: 0.4rem;
+    flex: 1 1 180px;
+}
+
+.filter-group label {
+    font-size: 0.75rem;
+    font-weight: 600;
+    color: var(--ink-muted);
+    text-transform: uppercase;
+    letter-spacing: 0.05em;
+}
+
+.filter-group input[type="date"] {
+    padding: 0.5rem 0.75rem;
+    border: 1px solid var(--border);
+    border-radius: var(--radius-sm);
+    font-family: var(--font-body);
+    font-size: 0.9rem;
+    color: var(--ink-soft);
+    background: var(--paper);
+    outline: none;
+    transition: border-color 0.2s, box-shadow 0.2s;
+    width: 100%;
+}
+
+.filter-group input[type="date"]:focus {
+    border-color: var(--accent);
+}
+
+.filter-actions {
+    display: flex;
+    align-items: center;
+    gap: 0.75rem;
+}
+
+.filter-actions .btn {
+    padding: 0.5rem 1rem;
+    border-radius: var(--radius-sm);
+    font-size: 0.9rem;
+    font-weight: 500;
+    cursor: pointer;
+    text-decoration: none;
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    transition: all 0.2s;
+    height: 38px;
+}
+
+.filter-actions .btn-primary {
+    background: var(--ink);
+    color: var(--paper);
+    border: none;
+}
+
+.filter-actions .btn-primary:hover {
+    background: var(--accent);
+}
+
+.filter-actions .btn-secondary {
+    background: transparent;
+    color: var(--ink-soft);
+    border: 1px solid var(--border);
+}
+
+.filter-actions .btn-secondary:hover {
+    background: var(--border-soft);
+    color: var(--ink);
+}
+
+/* Empty States */
+.empty-state {
+    padding: 2rem 1.5rem;
+    text-align: center;
+    background: var(--paper);
+    border: 1px dashed var(--border);
+    border-radius: var(--radius-sm);
+}
+
+.empty-message {
+    color: var(--ink-muted);
+    font-size: 0.875rem;
+}
+
+.empty-table-cell {
+    padding: 2.5rem 1.5rem !important;
+    text-align: center;
+}
+
 /* ------------------------------------------------------------------ */
 /* Responsive                                                          */
 /* ------------------------------------------------------------------ */
@@ -385,4 +525,22 @@
         grid-template-columns: 1fr;
         gap: 1rem;
     }
+    
+    .filter-form {
+        flex-direction: column;
+        align-items: stretch;
+        gap: 1rem;
+    }
+    
+    .filter-group {
+        flex: 1 1 auto;
+    }
+    
+    .filter-actions {
+        margin-top: 0.25rem;
+    }
+    
+    .filter-actions .btn {
+        flex: 1;
+    }
 }
diff --git a/templates/profile.html b/templates/profile.html
index 1e37543..12d8661 100644
--- a/templates/profile.html
+++ b/templates/profile.html
@@ -30,24 +30,56 @@
                 Category Breakdown
             </h3>
             <div class="breakdown-list">
-                {% for item in category_breakdown %}
-                <div class="breakdown-item">
-                    <div class="breakdown-info">
-                        <span class="breakdown-name">{{ item.category }}</span>
-                        <span class="breakdown-amount tabular-nums">€{{ "%.2f"|format(item.amount) }}</span>
+                {% if category_breakdown %}
+                    {% for item in category_breakdown %}
+                    <div class="breakdown-item">
+                        <div class="breakdown-info">
+                            <span class="breakdown-name">{{ item.category }}</span>
+                            <span class="breakdown-amount tabular-nums">€{{ "%.2f"|format(item.amount) }}</span>
+                        </div>
+                        <div class="progress-track">
+                            <div class="progress-fill bar-{{ item.class }}" style="width: {{ item.percentage }}%;"></div>
+                        </div>
+                        <span class="breakdown-percentage">{{ item.percentage }}% of total</span>
                     </div>
-                    <div class="progress-track">
-                        <div class="progress-fill bar-{{ item.class }}" style="width: {{ item.percentage }}%;"></div>
+                    {% endfor %}
+                {% else %}
+                    <div class="empty-state">
+                        <p class="empty-message">No expenses in this range</p>
                     </div>
-                    <span class="breakdown-percentage">{{ item.percentage }}% of total</span>
-                </div>
-                {% endfor %}
+                {% endif %}
             </div>
         </div>
     </aside>
 
     <!-- Main Section: Stats and Transactions -->
     <section class="profile-main">
+        <!-- Date Filter Card -->
+        <div class="profile-card filter-card">
+            <div class="filter-presets">
+                <a href="{{ url_for('profile', preset='7d') }}" class="preset-btn{% if preset == '7d' %} active{% endif %}">Last 7 Days</a>
+                <a href="{{ url_for('profile', preset='30d') }}" class="preset-btn{% if preset == '30d' %} active{% endif %}">Last 30 Days</a>
+                <a href="{{ url_for('profile', preset='this-month') }}" class="preset-btn{% if preset == 'this-month' %} active{% endif %}">This Month</a>
+                <a href="{{ url_for('profile', preset='all') }}" class="preset-btn{% if preset == 'all' %} active{% endif %}">All Time</a>
+            </div>
+            
+            <form method="GET" action="{{ url_for('profile') }}" class="filter-form">
+                <input type="hidden" name="preset" value="custom">
+                <div class="filter-group">
+                    <label for="start_date">Start Date</label>
+                    <input type="date" id="start_date" name="start_date" value="{{ start_date or '' }}">
+                </div>
+                <div class="filter-group">
+                    <label for="end_date">End Date</label>
+                    <input type="date" id="end_date" name="end_date" value="{{ end_date or '' }}">
+                </div>
+                <div class="filter-actions">
+                    <button type="submit" class="btn btn-primary">Apply</button>
+                    <a href="{{ url_for('profile') }}" class="btn btn-secondary">Clear</a>
+                </div>
+            </form>
+        </div>
+
         <!-- Summary Stats Grid -->
         <div class="stats-grid">
             <div class="profile-card stat-card">
@@ -98,16 +130,26 @@
                         </tr>
                     </thead>
                     <tbody>
-                        {% for expense in recent_expenses %}
-                        <tr>
-                            <td class="text-muted text-sm">{{ expense.date }}</td>
-                            <td class="font-medium">{{ expense.description }}</td>
-                            <td>
-                                <span class="badge badge-{{ expense.category|lower }}">{{ expense.category }}</span>
-                            </td>
-                            <td class="text-right font-semibold tabular-nums">€{{ "%.2f"|format(expense.amount) }}</td>
-                        </tr>
-                        {% endfor %}
+                        {% if recent_expenses %}
+                            {% for expense in recent_expenses %}
+                            <tr>
+                                <td class="text-muted text-sm">{{ expense.date }}</td>
+                                <td class="font-medium">{{ expense.description }}</td>
+                                <td>
+                                    <span class="badge badge-{{ expense.category|lower }}">{{ expense.category }}</span>
+                                </td>
+                                <td class="text-right font-semibold tabular-nums">€{{ "%.2f"|format(expense.amount) }}</td>
+                            </tr>
+                            {% endfor %}
+                        {% else %}
+                            <tr>
+                                <td colspan="4" class="empty-table-cell">
+                                    <div class="empty-state">
+                                        <p class="empty-message">No transactions found in this range</p>
+                                    </div>
+                                </td>
+                            </tr>
+                        {% endif %}
                     </tbody>
                 </table>
             </div>
